use crate::{data_structures::{LightSource, Model}, gl_wrappers::ShaderProgram};

pub struct SimpleRenderer {
    shader_program: ShaderProgram
    // Shader Input
}

impl SimpleRenderer {
pub fn draw(&self, model: &Model, light_source: &LightSource) {

}
}